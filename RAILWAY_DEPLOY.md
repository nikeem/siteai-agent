# Деплой на Railway через веб-интерфейс (пошаговая инструкция)

## 🚀 Простой способ деплоя на Railway

### Шаг 1: Создайте аккаунт Railway

1. Откройте https://railway.app/
2. Нажмите "Login" и войдите через GitHub
3. Перейдите в dashboard: https://railway.app/dashboard

### Шаг 2: Создайте новый проект

1. Нажмите "New Project" → "Deploy from GitHub repo"
2. Выберите репозиторий: `nikeem/siteai-agent`
3. Railway автоматически определит Python проект

### Шаг 3: Настройте проект

В настройках проекта Railway:

**Root Directory**: `backend`

**Build Command** (если не определён автоматически):
```bash
pip install -r requirements.txt
```

**Start Command**:
```bash
uvicorn app.main:app --host 0.0.0.0 --port $PORT
```

### Шаг 4: Добавьте Environment Variables

В разделе проекта Railway → Variables → New Variable:

```
OPENAI_API_KEY = sk-rQquJGhxsILSH91o4HLRjxpr2mZoIICe
OPENAI_BASE_URL = https://routerai.ru/api/v1
DEFAULT_MODEL = openai/o4-mini
PORT = 8000
```

Нажмите "Save Variables"

### Шаг 5: Запустите деплой

1. Нажмите "Deploy"
2. Railway автоматически задеплоит backend
3. Подождите 2-5 минут

### Шаг 6: Получите URL Backend

После успешного деплоя:

1. В Railway проекте откройте раздел "Domains"
2. Скопируйте URL (например): `https://siteai-agent-production.up.railway.app`

API будет доступен по:
```
https://siteai-agent-production.up.railway.app/api/v1
```

### Шаг 7: Проверьте работу

```bash
# Health check
curl https://siteai-agent-production.up.railway.app/api/v1/health
```

Должен вернуть:
```json
{"status":"ok","timestamp":"...","version":"0.1.0"}
```

---

## 🎨 Следующий шаг: Настроить Vercel

### 1. Откройте ваш проект на Vercel

https://vercel.com/nikeem/siteai-agent

### 2. Перейдите в Settings → Environment Variables

### 3. Добавьте переменную:

```
VITE_API_URL = https://siteai-agent-production.up.railway.app/api/v1
```

(замените на ваш реальный URL от Railway)

### 4. Редеплой

После добавления переменной Vercel автоматически передеплоит проект.

---

## ✅ Готово!

Теперь ваш AI агент полностью работает:
- Backend на Railway
- Frontend на Vercel
- Виджет на вашем сайте

---

## 🐛 Если что-то не работает

### Backend на Railway

- Проверьте логи: Railway → 你的项目 → Logs
- Проверьте переменные: Settings → Variables
- Убедитесь что PORT = 8000

### Frontend на Vercel

- Проверьте что VITE_API_URL правильный
- Откройте консоль браузера (F12)
- Ищите ошибки в Network tab

### CORS ошибки

Если видите CORS ошибки в консоли:

1. На Railway добавьте в Variables:
```
CORS_ORIGINS = https://your-project.vercel.app
```

2. Редеплойте Railway

---

## 📞 Нужна помощь?

Сообщите мне, какой шаг вызывает трудности!
