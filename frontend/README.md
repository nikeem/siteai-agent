# SiteAI Agent Widget - Frontend

Виджет AI-агента для консультирования посетителей сайта в реальном времени.

## Возможности

- 🤖 Умный консультант на базе AI
- 💬 Общение в реальном времени
- 📱 Адаптивный дизайн для мобильных устройств
- 🎨 Настраиваемый внешний вид
- 🔗 Лёгкая интеграция на любой сайт
- 🌐 Готовые лендинги для туризма (ToursLanding)

## Структура проекта

```
src/
├── components/
│   └── Widget/              # Компоненты чат-виджета
│       ├── ChatWidget.tsx   # Главный компонент виджета
│       ├── ChatWidget.css   # Стили виджета
│       └── ...
├── pages/
│   └── ToursLanding.tsx     # Лендинг "Туры в Турцию 2026"
│   └── ToursLanding.css     # Стили лендинга
├── services/                # API клиент
├── types/                   # TypeScript типы
├── utils/                   # Утилиты
└── main.tsx                 # Точка входа
```

## ToursLanding - Лендинг туров в Турцию

### Описание
Готовый продающий лендинг для раннего бронирования туров в Турцию на 2026 год с интеграцией AI-консультанта.

### Реализованные секции

**Фаза 1 - Базовая структура:**
- ✅ Header с логотипом и контактами
- ✅ Hero Section с CTA кнопкой
- ✅ Что входит в стоимость (4 иконки)
- ✅ Популярные курорты Турции (6 карточек)
- ✅ Footer

**Фаза 2 - УТП и доверие:**
- ✅ Преимущества раннего бронирования (6 блоков)
- ✅ Три шага (How it works)
- ✅ Топ-5 отелей Турции

**Фаза 3 - Социальные доказательства:**
- ✅ Наши эксперты по Турции (6 менеджеров)
- ✅ Отзывы туристов (3 отзыва)
- ✅ FAQ (6 вопросов с аккордеоном)
- ✅ Офисы и контакты

### Техническая оптимизация

**Рефакторинг CSS (выполнен):**
- Удалены дубликаты селекторов (было 9, осталось 0)
- Оптимизирован размер файла: -15.6% (с 35.33KB до 29.83KB)
- Все grid-сетки используют адаптивный `auto-fit`
- Удалены неиспользуемые стили
- Добавлены базовые стили для кнопок `.tours-primary-cta`

**Метрики:**
- 1531 строка CSS
- 106 уникальных селекторов `.tours-`
- Production build: 31.26 KB (сжатый)
- Полностью адаптивный дизайн (mobile-first)

### Использование

```tsx
import { ToursLanding } from './pages/ToursLanding';

function App() {
  return <ToursLanding />;
}
```

## Документация

- **[REFACTORING_REPORT.md](./REFACTORING_REPORT.md)** - Подробный отчёт о рефакторинге CSS
  - Найденные проблемы (дубликаты, конфликты)
  - Выполненная работа (удаление 393 строк)
  - Результаты оптимизации (-15.6% размера)
  - Исправление пост-рефакторинга

## Установка зависимостей

```bash
npm install
```

## Разработка

```bash
npm run dev
```

Виджет будет доступен по адресу: http://localhost:3000

## Сборка для продакшена

```bash
npm run build
```

Результат будет в директории `dist/`

## Проверка типов

```bash
npm run type-check
```

## Линтинг

```bash
npm run lint
```

## Интеграция на сайт

### Вариант 1: NPM пакет

```bash
npm install siteai-widget
```

```tsx
import { ChatWidget } from 'siteai-widget';

function App() {
  return (
    <ChatWidget siteId="your-site-id" />
  );
}
```

### Вариант 2: CDN

```html
<script src="https://cdn.siteai.ai/widget.js"></script>
<script>
  SiteAIWidget.init({
    siteId: 'your-site-id',
    autoOpen: false
  });
</script>
```

## Конфигурация

Виджет поддерживает следующие пропсы:

- `siteId` - ID вашего сайта (получается в админ-панели)
- `autoOpen` - автоматически открывать чат при загрузке (по умолчанию: false)

## Интеграция виджета на лендинг

ChatWidget автоматически интегрируется в ToursLanding:

```tsx
// В ToursLanding.tsx
import { ChatWidget } from '../components/Widget/ChatWidget';

// Внутри компонента
<ChatWidget ref={chatWidgetRef} siteId="tours_turkey" />
```

Все CTA кнопки на лендинге вызывают функцию `scrollToChat()`, которая:
1. Плавно скроллит к чат-виджету
2. Устанавливает контекстное сообщение
3. Фокусирует поле ввода

## История коммитов

Основные вехи развития проекта:

- **7682dc1** - Унификация стилей кнопок (все кнопки синие)
- **95ef9d0** - Добавление базовых стилей для .tours-primary-cta
- **9f895c5** - Восстановление секции курортов после рефакторинга
- **af8de57** - Завершение Фазы 3 (Experts, Reviews, FAQ, Offices)
- **Рефакторинг** - Оптимизация CSS (-393 строки, -15.6% размера)
