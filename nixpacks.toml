[phases.build]
cmds = ["cd backend && pip install --no-cache-dir -r requirements.txt"]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[phases.install]
cmds = ["cd backend && python -m pip install --upgrade pip"]
